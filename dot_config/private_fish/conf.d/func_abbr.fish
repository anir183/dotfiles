#!/usr/bin/env fish

#[[ abbreviations for functions as i dont like underscores in commands ]]
#[[ but i also dislike hyphens in file names ]]

# install_packages
abbr -a install-packages install_packages

# is_distrobox
abbr -a is-distrobox is_distrobox

# link_host_bin
abbr -a link-host-bin link_host_bin

# sess_zellij
abbr -a sess-zellij sess_zellij
abbr -a sess "sess_zellij --all"

# update_mirrors
abbr -a update-mirrors update_mirrors
